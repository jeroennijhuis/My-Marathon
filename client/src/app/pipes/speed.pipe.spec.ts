/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SpeedPipe } from './speed.pipe';

describe('Pipe: Speede', () => {
  it('create an instance', () => {
    let pipe = new SpeedPipe();
    expect(pipe).toBeTruthy();
  });
});
